# App mode
APP_NAME=tls-cert-generator
FLASK_CONFIG=development
LOG_LEVEL=INFO

# Rate limiting
RATE_LIMIT_PER_IP=10/minute

# TLS and proxy behavior
TLS_MODE=proxy_terminated
TRUST_PROXY_HOPS=1

# Runtime
HOST=0.0.0.0
PORT=8000
